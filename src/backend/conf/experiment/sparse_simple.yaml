# @package _global_

# Sparse reward with discrete actions - simplest baseline
# Good for: Initial testing, sparse reward benchmarks

grid_size: [128, 128]
source_location: [100, 100]
max_steps: 1000
goal_radius: 5.0
plume_sigma: 20.0
action_type: discrete
step_size: 1
observation_type: concentration
reward_type: sparse
