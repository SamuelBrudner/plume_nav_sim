"""Qt widget modules for plume_nav_debugger.

These classes were originally defined in `plume_nav_debugger.main_window` and were
split into per-widget modules to keep files small and easier to test/maintain.
"""

